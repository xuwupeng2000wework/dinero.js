import { Currency } from "../types/Currency";

/**
 * Salvadoran colón
 */
const SVC: Currency = {
  name: "colón",
  sign: "₡",
  code: {
    alpha: "SVC",
    num: "222"
  },
  exponent: 2
};

export default SVC;
