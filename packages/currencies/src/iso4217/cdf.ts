import { Currency } from "../types/Currency";

/**
 * Congolese franc
 */
const CDF: Currency = {
  name: "franc",
  sign: "FC",
  code: {
    alpha: "CDF",
    num: "976"
  },
  exponent: 2
};

export default CDF;
