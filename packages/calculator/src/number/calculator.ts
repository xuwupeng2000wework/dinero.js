import { add, subtract, multiply, divide, modulo, round } from "./operations";

/**
 * A calculator for numbers.
 */
const calculator = {
  add,
  subtract,
  multiply,
  divide,
  modulo,
  round
};

export default calculator;
